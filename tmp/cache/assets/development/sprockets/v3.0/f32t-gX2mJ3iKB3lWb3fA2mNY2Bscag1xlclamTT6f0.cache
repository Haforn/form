I""(function() {
  $(function() {
    $('.new_comment').on('keyup', function(evt) {
      if (evt.which === 13) {
        evt.preventDefault();
        $(this).submit();
        return false;
      }
    });
    return $('.new_comment').on('ajax:success', function(evt, post, status) {
      var $comment;
      $(this)[0].reset();
      $(this).find('textarea').focus();
      $comment = $('<div/>').addClass('comment').html(post.body);
      $comment.append($('<hr/>'));
      return $('.comments').append($comment);
    });
  });

}).call(this);
:ET